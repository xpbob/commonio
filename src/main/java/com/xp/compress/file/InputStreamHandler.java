package com.xp.compress.file;

import java.io.InputStream;

public interface InputStreamHandler {
	/**
	 * the input stream need to close
	 * @param in
	 */
	public void getInputStream(InputStream in);
}
